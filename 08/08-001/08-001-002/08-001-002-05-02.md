# 作用域 08-11 视频day05 Python文献

- 查找顺序：*局部作用域 > 嵌套作用域 > 全局作用域 > 内建作用域*
    - 会引入作用域：模块、类、函数。
        - 局部作用域 (Local)：在函数内定义。
        - 嵌套作用域 (Enclosing)：外层函数的局部作用域。
        - 全局作用域 (Global)：在模块内、函数外定义。
        - 内建作用域 (Built-in)：Python定义。
    - 不会引入作用域（外部可以访问在这些代码块中定义的变量）：分支、循环、异常处理。
