# 文件读写 03-04 视频day06 Python文献

- 步骤：
    1. 打开或创建文件。
    2. 操作文件。
    3. 关闭文件。
- `file.write(s)`：向磁盘文件中写入数据。
- `file.read(size=-1)`：从磁盘文件中读取数据，返回`str`或`bytes`。
    - `size`：要读取的字符数或字节数。默认为`-1`表示读取所有内容。
