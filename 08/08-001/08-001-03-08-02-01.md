# 小整数池

- CPython会提前创建`[-5, 256]`的整数对象，并放入小整数对象池。
- 这些小整数对象不会被垃圾回收，避免为小整数频繁申请和销毁内存空间。
- 不管在程序的什么位置，使用位于这个范围内的小整数都是同一个对象。

---

- xx池：缓存技术：
    - xx对象的创建和销毁比较消耗资源。
    - 在程序启动时，提前创建多个xx对象：
        - 要用时，从池子里取出xx对象。
        - 不用时，把xx对象放回池子里。
